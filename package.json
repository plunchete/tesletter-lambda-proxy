{
  "name": "tesletter-lambda-proxy",
  "version": "0.1",
  "description": "CORS proxy for AWS lambda created for https://tesletter.com",
  "main": "handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ignacio Andreu",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.18.0",
  },
  "devDependencies": {
    "serverless-offline": "^4.9.4"
  }
}
